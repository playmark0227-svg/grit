# GRIT 公開手順書(SETUP.md)

このサイトは、何も設定しなくても **デモモード**(サンプルデータ表示)で動きます。
実際にインターネットに公開して、商品登録・ブログ投稿・Stripe決済を動かすための手順を、初めての方向けにまとめました。

上から順番に進めてください。所要時間はだいたい 1〜2 時間です。

## まずローカル(自分のパソコン)で見てみる

公開の前に、手元でデザインを確認できます。ターミナルでこのフォルダに移動して:

```bash
python3 serve.py
```

ブラウザで `http://localhost:5178` を開いてください(止めるときはターミナルで `Ctrl + C`)。
この状態はデモモードなので、サンプル商品・サンプル記事が表示されます。`/admin/` も「Googleでログイン」を押すと仮ログインして操作を試せます(保存は画面内だけの仮保存)。

> ファイルを直接ダブルクリックして開く(`file://`)方法では動きません。ページの読み込み方式(ES モジュール)の制約なので、必ず上のコマンドで開いてください。

## 必要なもの

- Google アカウント(Firebase 用。管理者ログインにも使います)
- クレジットカード(Firebase の Blaze プラン登録と Stripe に必要。**無料枠内なら請求はほぼ0円**です。後述)
- パソコン(Mac / Windows どちらでも可。コマンド入力が数回あります)

## 全体の流れ

1. Firebase プロジェクトを作る
2. Web アプリを登録して設定を `firebase-config.js` に貼る
3. Authentication(Google ログイン)を有効化する
4. Firestore / Storage を有効化する
5. Blaze プランに変更する
6. Firebase CLI でサイトを公開(デプロイ)する
7. Stripe を設定する(テスト環境)
8. テストカードで購入テストをする
9. 本番切替チェックリスト
10. 管理者メールアドレスの変更方法
11. よくあるエラーと対処
12. スマホから商品・ブログを投稿する
13. 注文の確認と発送・送料の設定

---

## 1. Firebase プロジェクトを作る

1. ブラウザで [Firebase コンソール](https://console.firebase.google.com/) を開き、Google アカウントでログインします。
2. 「プロジェクトを作成」(または「プロジェクトを追加」)をクリック。
3. プロジェクト名に `grit-shop` などお好きな名前を入力します。
   - 入力欄の下に小さく表示される **プロジェクトID**(例: `grit-shop-4a2b1`)を必ずメモしてください。あとで何度も使います。IDは後から変更できません。
4. Google アナリティクスは「有効にしない」でOK(あとから追加できます)。
5. 「プロジェクトを作成」を押して、完了まで待ちます。

### .firebaserc の書き換え(重要)

このフォルダ直下にある `.firebaserc` を開くと、仮のID `grit-shop` が書かれています。
**手順3でメモした自分のプロジェクトIDに書き換えて保存してください。**

```json
{
  "projects": {
    "default": "ここを自分のプロジェクトIDに"
  }
}
```

---

## 2. Web アプリを登録して firebase-config.js に貼る

1. Firebase コンソールのプロジェクトトップで、中央の **`</>`(ウェブ)アイコン** をクリック。
2. アプリのニックネームに `grit-web` など適当な名前を入れて「アプリを登録」。
   - 「Firebase Hosting も設定します」のチェックは **入れなくてOK**(Hosting は手順6でコマンドから設定します)。
3. 画面に以下のようなコードが表示されます:

```js
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "grit-shop-4a2b1.firebaseapp.com",
  projectId: "grit-shop-4a2b1",
  storageBucket: "grit-shop-4a2b1.firebasestorage.app",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef123456"
};
```

4. `public/js/firebase-config.js` をテキストエディタで開き、`window.FIREBASE_CONFIG = { ... }` の中身を、表示された値で**そのまま置き換えて**保存します。

```js
window.FIREBASE_CONFIG = {
  apiKey: "AIzaSy...",             // ← コンソールに表示された値をそのまま
  authDomain: "grit-shop-4a2b1.firebaseapp.com",
  projectId: "grit-shop-4a2b1",
  storageBucket: "grit-shop-4a2b1.firebasestorage.app",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef123456",
};
```

> ここを貼り替えるまでサイトはデモモードで動きます。逆に、貼り替えた瞬間から本物の Firebase に接続します(最初は商品0件・記事0件の状態です)。
>
> この設定値(apiKey など)は「秘密のパスワード」ではなく、Webサイトに埋め込む前提の公開情報です。流出を心配する必要はありません。守りは手順6で公開するセキュリティルールが担当します。

---

## 3. Authentication(Google ログイン)を有効化する

管理ページ(`/admin/`)のログインに使います。

1. Firebase コンソール左メニュー「構築」→「Authentication」→「始める」。
2. 「ログイン方法」タブ →「Google」を選択 → 右上のスイッチを **有効** に。
3. 「プロジェクトのサポートメール」に自分のメールを選んで「保存」。

管理者としてログインできるのは、コードに登録されたメールアドレスだけです(初期設定: `playmark0227@gmail.com`)。
**別のメールに変えたい場合は手順10** を見てください。

---

## 4. Firestore / Storage を有効化する

### Firestore(データベース: 商品・ブログ・注文の保存先)

1. 左メニュー「構築」→「Firestore Database」→「データベースを作成」。
2. ロケーションは **`asia-northeast1`(東京)** を選択(あとから変更できません)。
3. モードは「**本番環境モード**」を選択して作成。
   - ルール(誰が読み書きできるか)は手順6でこのフォルダの `firestore.rules` をデプロイして上書きするので、ここでの選択はどちらでも動きますが、本番環境モードが安全です。

### Storage(商品写真・ブログ画像の保存先)

1. 左メニュー「構築」→「Storage」→「始める」。
2. ロケーションは Firestore と同じ **`asia-northeast1`** を選択。

> **注意**: 2024年10月以降に作成したプロジェクトでは、Storage の有効化に Blaze プラン(手順5)が必要です。「アップグレードが必要」と表示されたら、先に手順5を済ませてからここに戻ってきてください。

---

## 5. Blaze プラン(従量課金)に変更する

決済機能(Cloud Functions)を使うために必要です。

1. Firebase コンソール左下の「アップグレード」(または歯車 → 使用量と請求額)→「Blaze(従量課金)」を選択。
2. 請求先アカウント(クレジットカード)を登録して完了。

### 「有料」といっても実質無料です

Blaze プランにも毎月の**無料枠**が含まれています(Functions 呼び出し 200万回/月、Firestore 読み取り 5万回/日 など)。
小規模な古着屋ECのアクセス量なら、まず無料枠に収まり **請求はほぼ0円** です。

心配な場合は、Google Cloud コンソールの「予算とアラート」で月額 500 円などの予算アラートを設定しておくと、超えそうなときにメールで知らせてくれます。

---

## 6. Firebase CLI でサイトを公開(デプロイ)する

### 6-1. Node.js を入れる

ターミナル(Mac: 「ターミナル」アプリ / Windows: PowerShell)を開き、以下を実行:

```bash
node -v
```

`v18` 以上のバージョン番号が出ればOK。「command not found」なら [Node.js 公式サイト](https://nodejs.org/ja) から **LTS版** をダウンロードしてインストールし、ターミナルを開き直してください。

### 6-2. Firebase CLI(firebase-tools)を入れてログイン

```bash
npm install -g firebase-tools
firebase login
```

ブラウザが開くので、手順1と同じ Google アカウントでログインして許可します。

### 6-3. Functions の依存パッケージを入れる

ターミナルで **このフォルダ(SETUP.md がある場所)** に移動してから:

```bash
cd functions
npm install
cd ..
```

> フォルダへの移動は `cd ` と入力したあとに、Finder/エクスプローラーからこのフォルダをターミナルにドラッグ&ドロップすると簡単です。

### 6-4. まずサイトとルールを公開

このフォルダ(SETUP.md がある場所)で:

```bash
firebase deploy --only hosting,firestore,storage
```

成功すると `Hosting URL: https://あなたのプロジェクトID.web.app` と表示されます。
ブラウザで開いてみてください。**あなたのサイトがもう世界に公開されています。**

- この時点では商品0件・記事0件です。`/admin/` を開いて Google ログインし、商品やブログを登録してみましょう。
- 決済(レジへ進む)はまだ動きません。次の手順7で設定します。

> このコマンドは、サイト本体(`public/`)と一緒に `firestore.rules` / `firestore.indexes.json` / `storage.rules` も公開しています。セキュリティとブログ用の検索インデックスがここで有効になります。

---

## 7. Stripe を設定する(テスト環境)

クレジットカード決済には [Stripe](https://stripe.com/jp)(ストライプ)を使います。世界中のECで使われている決済サービスです。

### 7-1. Stripe アカウントを作る

1. [Stripe](https://dashboard.stripe.com/register) でアカウント登録(メールアドレスでOK)。
2. 登録直後は「**テストモード**」で使えます。本番でお金を受け取るには後で本人確認・事業情報の登録(本番申請)が必要ですが、**テストは今すぐできます**。

### 7-2. テスト用のシークレットキーを取得

1. Stripe ダッシュボード右上が「テストモード」(オレンジ表示)になっていることを確認。
2. 「開発者」→「APIキー」を開く。
3. 「シークレットキー」の `sk_test_...` で始まるキーを表示してコピー。
   - `pk_test_...`(公開可能キー)ではなく **`sk_test_...`** の方です。
   - このキーは**絶対に人に見せない・ファイルに書き込まない**でください。次の手順で Firebase の金庫(Secret Manager)に直接しまいます。

### 7-3. キーを Firebase の Secret に登録

このフォルダで以下を実行し、聞かれたら値を貼り付けて Enter:

```bash
firebase functions:secrets:set STRIPE_SECRET_KEY
```

→ `sk_test_...` を貼り付け。

```bash
firebase functions:secrets:set STRIPE_WEBHOOK_SECRET
```

→ まだ本当の値がないので、仮に `temp` と入力しておきます(7-5 で本物に差し替えます)。

### 7-4. Functions をデプロイ

```bash
firebase deploy --only functions
```

初回は数分かかります(「APIを有効にしますか?」と聞かれたら `Y`)。
成功すると、ターミナルに関数のURLが表示されます:

```
Function URL (stripeWebhook(asia-northeast1)): https://stripewebhook-xxxxxxxx-an.a.run.app
```

この **stripeWebhook の URL をコピー**してください。
(見逃した場合は Firebase コンソール →「構築」→「Functions」の一覧でも確認できます)

### 7-5. Stripe に Webhook を登録

「決済が完了した」「期限切れになった」を Stripe からサイトに知らせてもらう設定です。**これをしないと、購入されても商品が SOLD になりません。**

1. Stripe ダッシュボード(テストモードのまま)→「開発者」→「Webhook」→「エンドポイントを追加」。
2. 「エンドポイントURL」に、7-4 でコピーした **stripeWebhook の URL** を貼り付け。
3. 「リッスンするイベントの選択」で以下の **2つ** を選ぶ:
   - `checkout.session.completed`(決済完了)
   - `checkout.session.expired`(決済画面の期限切れ)
4. 「イベントを追加」→「エンドポイントを追加」で保存。
5. 作成したエンドポイントの詳細画面にある「**署名シークレット**」(`whsec_...`)を表示してコピー。
6. ターミナルに戻って本物の値に差し替え:

```bash
firebase functions:secrets:set STRIPE_WEBHOOK_SECRET
```

→ `whsec_...` を貼り付け。

7. **Secret を差し替えたら再デプロイが必要**です:

```bash
firebase deploy --only functions
```

これで決済まわりの設定は完了です。

---

## 8. テストカードで購入テストをする

Stripe のテストモードでは、本物のカードの代わりに**テスト専用カード番号**を使います。お金は1円も動きません。

1. `https://あなたのプロジェクトID.web.app/admin/` で商品を1つ登録します(写真・名前・価格など)。
2. サイトの SHOP でその商品を開き、「カートに入れる」→ カート →「レジへ進む」。
3. Stripe の決済ページが開いたら、以下を入力:
   - カード番号: **`4242 4242 4242 4242`**
   - 有効期限: 未来の年月なら何でも(例 `12/34`)
   - CVC: 何でも(例 `123`)
   - 名前・メール: 何でもOK
4. 支払いを完了すると `「ご購入ありがとうございます」` のページに戻ります。

> 決済ページでは、氏名・**配送先住所**・電話番号・メールアドレスをお客様に入力してもらうよう設定済みです(日本国内向け)。入力された宛先は管理ページの「注文」タブに表示されます(手順13)。

### 確認ポイント

- SHOP に戻ると、その商品が **SOLD** 表示になっている(Webhook が動いた証拠)
- 管理ページ(`/admin/`)の「**注文**」タブに、購入内容と配送先が並んでいる
- Firebase コンソール → Firestore → `orders` コレクションに注文が記録されている
- Stripe ダッシュボード →「支払い」にテスト決済が並んでいる

### あわせて試すと安心

- 決済ページで「←戻る」した場合 → カートは残り、商品は約30分後に自動で購入可能に戻ります(`checkout.session.expired` の Webhook で解放)。すぐ戻したい場合は管理ページから status を戻せます。
- わざと決済に失敗するカード: `4000 0000 0000 0002`(常に拒否されるテストカード)

---

## 9. 本番切替チェックリスト

テストで問題なければ、本物のカードで決済できるようにします。

- [ ] **Stripe の本番申請を完了する**: ダッシュボードの案内に従って事業情報・本人確認・銀行口座(売上の入金先)を登録し、本番利用を有効化する。
- [ ] **本番のシークレットキーに差し替える**: ダッシュボードを「本番モード」に切り替え →「開発者」→「APIキー」→ `sk_live_...` をコピーして

  ```bash
  firebase functions:secrets:set STRIPE_SECRET_KEY
  ```

- [ ] **本番モードでも Webhook を登録する**: テストモードと本番モードの Webhook 設定は**別物**です。手順 7-5 と同じことを「本番モード」で行い(URLは同じ、イベントも同じ2つ)、本番の `whsec_...` を

  ```bash
  firebase functions:secrets:set STRIPE_WEBHOOK_SECRET
  ```

- [ ] **再デプロイ**(Secret 差し替え後は必須):

  ```bash
  firebase deploy --only functions
  ```

- [ ] **送料を決めて設定する**(手順13)。初期設定は「送料無料(商品代金のみ)」です。
- [ ] テストで作った商品・`orders` のテストデータを整理する(Firestore コンソールから削除できます)。
- [ ] 本物のカードで少額の商品を1つ購入 → SOLD 化と入金予定を確認 → Stripe ダッシュボードから返金してキャンセル。
- [ ] ネットショップとして継続運用する場合は「特定商取引法に基づく表記」「プライバシーポリシー」のページ掲載もご検討ください(法律上必要になる場合があります)。

---

## 10. 管理者メールアドレスの変更方法

管理者(商品・ブログを投稿できる人)の Google アカウントは、**次の3ヶ所に同じ内容で**書かれています。変更・追加するときは必ず3ヶ所すべてを揃えて書き換えてください。

| ファイル | 場所 |
|---|---|
| `public/js/store.js` | 上部の `ADMIN_EMAILS = ["playmark0227@gmail.com"]` |
| `firestore.rules` | `isAdmin()` 内の `['playmark0227@gmail.com']` |
| `storage.rules` | `isAdmin()` 内の `['playmark0227@gmail.com']` |

複数人にする場合はカンマ区切りで追加します(3ヶ所とも):

```js
["playmark0227@gmail.com", "staff@example.com"]
```

書き換えたら反映のためデプロイ:

```bash
firebase deploy --only hosting,firestore,storage
```

---

## 11. よくあるエラーと対処

### サイトがずっとデモモード(サンプル商品)のまま
`public/js/firebase-config.js` の貼り付けができていません。`PASTE_YOUR_API_KEY` が残っていないか確認し、貼り替えたら再度 `firebase deploy --only hosting`。

### 「Missing or insufficient permissions」と出る
セキュリティルールが未デプロイか、管理者メールが一致していません。
`firebase deploy --only firestore,storage` を実行。それでも出る場合は、ログイン中の Google アカウントのメールと手順10の3ヶ所が完全に一致しているか(大文字小文字・タイプミス)確認。

### 「The query requires an index」と出る
ブログ用の複合インデックスが未作成です。`firebase deploy --only firestore` を実行(`firestore.indexes.json` が反映されます)。反映後、インデックス構築に数分かかることがあります。

### 管理ページで「権限がありません」と出る
ログインした Google アカウントが `ADMIN_EMAILS` に入っていません。手順10を確認。

### Google ログインのポップアップが開かない/すぐ閉じる
ブラウザのポップアップブロックを解除してください。独自ドメインで運用している場合は、Firebase コンソール → Authentication →「設定」→「承認済みドメイン」にそのドメインを追加します(`web.app` / `firebaseapp.com` は最初から承認済み)。

### 「レジへ進む」でエラーになる
- 「デモモードでは決済できません」→ firebase-config.js 未設定(手順2)。
- 「売り切れの商品が含まれています」→ 誰かが先に購入・予約した1点物です。カートから外してください(仕様どおりの動作です)。
- その他のエラー → Functions が未デプロイ(手順7-4)、`STRIPE_SECRET_KEY` 未設定(手順7-3)、または `http://` でサイトを開いている(必ず `https://〜.web.app` で開く)。Firebase コンソール → Functions → ログでエラー内容を確認できます(`firebase functions:log` でも可)。

### 購入されたのに商品が SOLD にならない
Webhook が届いていません。
- Stripe ダッシュボード →「開発者」→「Webhook」→ エンドポイントの「イベントの配信」で失敗履歴を確認。
- `400` エラーが並んでいる → `STRIPE_WEBHOOK_SECRET` が間違っているか、差し替え後の再デプロイ(手順7-5の最後)を忘れています。
- テストモードで購入したのに本番モードの Webhook 画面を見ている(またはその逆)ケースもよくあります。

### 画像のアップロードに失敗する
- Storage が未有効化(手順4)/ `storage.rules` 未デプロイ。
- 管理者以外のアカウントでログインしている(手順10)。
- 1ファイル 5MB の上限を超えている(通常はアプリ側で自動圧縮されるため、まず起きません)。

### `firebase: command not found`
`npm install -g firebase-tools` が終わっているか確認し、ターミナルを開き直してください。Mac で権限エラーが出る場合は `sudo npm install -g firebase-tools`。

### デプロイ時に「Error: Not in a Firebase app directory」
コマンドを実行する場所が違います。`firebase.json` があるこのフォルダに `cd` で移動してから実行してください。

### Functions のデプロイで課金関連のエラーが出る
Blaze プラン(手順5)への変更が必要です。

---

## 12. スマホから商品・ブログを投稿する

管理ページはスマホ最優先で作ってあります。店頭で撮ってそのまま投稿できます。

1. スマホのブラウザで `https://あなたのプロジェクトID.web.app/admin/` を開く。
2. 「Googleでログイン」→ 管理者の Google アカウントでログイン。
3. 「+ 新規商品」→ カメラで撮影 or アルバムから写真を選択 → 名前・価格などを入れて保存。

### ホーム画面に追加しておくと便利

アプリのように1タップで開けます。

- **iPhone(Safari)**: 管理ページを開いた状態で、下の「共有」ボタン(四角に↑)→「ホーム画面に追加」。
- **Android(Chrome)**: 右上の「⋮」メニュー →「ホーム画面に追加」(または「アプリをインストール」)。

---

## 13. 注文の確認と発送・送料の設定

### 注文を確認する

管理ページ(`/admin/`)の「**注文**」タブに、お支払いが完了した注文が新しい順で並びます。
1件ごとに、購入された商品・お支払い合計・**お名前・配送先住所・電話番号・メールアドレス**が表示されます。

- 「**宛先をコピー**」を押すと、氏名・住所・電話番号をまとめてコピーできます(配送伝票への入力用)。
- 発送したら「**発送済みにする**」を押してください。上部に「未発送○件」と出るので、作業漏れを防げます。
- スマホのホーム画面に `https://あなたのプロジェクトID.web.app/admin/#orders` を追加しておくと、1タップで注文一覧を開けます。

> 注文データは Cloud Functions が自動で作ります。管理ページから注文を作ったり消したりはできません(金額の偽装を防ぐため、セキュリティルールで禁止しています)。返金・キャンセルは Stripe ダッシュボードから行ってください。

### 送料を設定する

初期設定は **送料無料**(お客様のお支払いは商品代金のみ)です。送料を取る場合:

1. `functions/index.js` の上のほうにある次の行を書き換えます。

   ```js
   const SHIPPING_FEE_YEN = 0;   // 0 = 送料無料 / 例: 800 → 全国一律800円
   ```

2. 保存したら再デプロイします。

   ```bash
   firebase deploy --only functions
   ```

これで決済画面に「全国一律送料」として加算されます。金額はお客様の支払い時に計算されるので、商品価格をいじる必要はありません。

> 「○円以上で送料無料」のような条件つきにしたい場合は、この定数だけでは表現できません。必要になったらご相談ください。

---

これで全手順は完了です。お店らしく商品とブログを充実させていきましょう。
分からないことが出たら、まず手順11(よくあるエラー)を見てみてください。
